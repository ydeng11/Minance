---
phase: 02-visual-consistency-table-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/src/app/transactions/page.tsx, apps/web/src/app/transactions/TransactionsCommandBar.tsx]
autonomous: true
requirements: [VISL-02, UFBK-03]
user_setup: []
must_haves:
  truths:
    - "User sees skeleton placeholders while transactions are loading"
    - "User sees spinner on Apply button during filter operation"
    - "User understands when data operations are in progress"
  artifacts:
    - path: "apps/web/src/app/transactions/page.tsx"
      provides: "Skeleton loading rows in table body"
      contains: "animate-pulse skeleton rows during loading"
    - path: "apps/web/src/app/transactions/TransactionsCommandBar.tsx"
      provides: "Spinner on Apply button during async operations"
      contains: "Loader2 with animate-spin"
  key_links:
    - from: "apps/web/src/app/transactions/page.tsx"
      to: "loading state"
      via: "useState loading variable"
      pattern: "loading === true"
    - from: "apps/web/src/app/transactions/TransactionsCommandBar.tsx"
      to: "Apply button"
      via: "onClick onApply"
      pattern: "Loader2"
---

<objective>
Add loading state visual indicators: skeleton placeholders for table rows during data fetch and spinner on Apply button during filter operations.

Purpose: Users always understand when data operations are in progress, preventing confusion about system state.
Output: Skeleton rows during loading, Loader2 spinner on Apply button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-consistency-table-polish/02-CONTEXT.md
@.planning/phases/02-visual-consistency-table-polish/02-RESEARCH.md

<interfaces>
<!-- Key patterns executor needs -->

From apps/web/src/app/transactions/page.tsx line 146:
```typescript
const [loading, setLoading] = useState(false);
```

From apps/web/src/app/import/page.tsx (Loader2 pattern reference):
```typescript
import { Loader2 } from "lucide-react";
// Usage: <Loader2 className="h-4 w-4 animate-spin" />
```

From apps/web/src/app/transactions/TransactionsCommandBar.tsx line 150-158:
```typescript
<button
  type="button"
  onClick={onApply}
  data-testid="txn-apply"
  className="inline-flex h-11 items-center justify-center rounded-2xl border border-emerald-500/30 bg-emerald-500/15 px-4 text-sm font-medium text-emerald-100 transition hover:bg-emerald-500/20"
>
  Apply
</button>
```

From apps/web/src/app/transactions/page.tsx lines 1146-1317:
```typescript
<tbody className="divide-y divide-neutral-900/80">
  {ledgerTransactions.map((txn) => { ... })}
  {!loading && ledgerTransactions.length === 0 ? ( ... ) : null}
</tbody>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading rows to transaction table (VISL-02)</name>
  <files>apps/web/src/app/transactions/page.tsx</files>
  <read_first>
    - apps/web/src/app/transactions/page.tsx lines 1140-1320 (table body section)
    - apps/web/src/app/transactions/page.tsx line 146 (loading state)
  </read_first>
  <action>
    Per D-13, D-14, D-15, D-16 decisions: Add skeleton placeholders during loading.
    
    In the `<tbody>` section (around line 1146), add skeleton rows before the actual transaction rows:
    
    ```typescript
    <tbody className="divide-y divide-neutral-900/80">
      {loading ? (
        <>
          {[...Array(5)].map((_, i) => (
            <tr key={`skeleton-${i}`} className="animate-pulse">
              <td className="px-5 py-5">
                <div className="h-4 w-4 rounded bg-neutral-800" />
              </td>
              <td className="px-5 py-5">
                <div className="h-4 w-20 rounded bg-neutral-800" />
              </td>
              <td className="px-5 py-5">
                <div className="space-y-2">
                  <div className="h-4 w-32 rounded bg-neutral-800" />
                  <div className="h-3 w-48 rounded bg-neutral-800" />
                </div>
              </td>
              <td className="px-5 py-5">
                <div className="h-6 w-20 rounded-xl bg-neutral-800" />
              </td>
              <td className="px-5 py-5">
                <div className="space-y-1">
                  <div className="h-4 w-24 rounded bg-neutral-800" />
                  <div className="h-3 w-16 rounded bg-neutral-800" />
                </div>
              </td>
              <td className="px-5 py-5">
                <div className="space-y-1">
                  <div className="h-4 w-16 rounded bg-neutral-800" />
                  <div className="h-3 w-12 rounded bg-neutral-800" />
                </div>
              </td>
              <td className="px-5 py-5 text-right">
                <div className="h-4 w-20 rounded bg-neutral-800" />
              </td>
              <td className="px-5 py-5 text-right">
                <div className="flex justify-end gap-2">
                  <div className="h-8 w-14 rounded-xl bg-neutral-800" />
                  <div className="h-8 w-16 rounded-xl bg-neutral-800" />
                </div>
              </td>
            </tr>
          ))}
        </>
      ) : (
        <>
          {ledgerTransactions.map((txn) => {
            // ... existing transaction row rendering ...
          })}
          {!loading && ledgerTransactions.length === 0 ? (
            // ... existing empty state ...
          ) : null}
        </>
      )}
    </tbody>
    ```
    
    Key aspects per D-14, D-15:
    - Skeleton style: dark neutral-800 background, subtle pulse animation
    - Show 5 skeleton rows (Claude's discretion - matches typical viewport)
    - Skeleton dimensions match actual row layout (prevent layout shift)
    - Preserve existing loading state pattern (D-16)
  </action>
  <acceptance_criteria>
    - page.tsx contains skeleton rows with `animate-pulse`
    - Skeleton rows render when `loading === true`
    - Skeleton row has 5 iterations
    - Skeleton cells use `bg-neutral-800` background
  </acceptance_criteria>
  <verify>
    <automated>grep -c "animate-pulse" apps/web/src/app/transactions/page.tsx | grep -q "1"</automated>
  </verify>
  <done>Skeleton placeholders appear during data loading</done>
</task>

<task type="auto">
  <name>Task 2: Add loading spinner to Apply button (UFBK-03)</name>
  <files>apps/web/src/app/transactions/TransactionsCommandBar.tsx</files>
  <read_first>
    - apps/web/src/app/transactions/TransactionsCommandBar.tsx (full file)
    - apps/web/src/app/import/page.tsx (Loader2 pattern reference)
  </read_first>
  <action>
    Per D-17 decision: Add Loader2 spinner to Apply button during async operations.
    
    Changes needed:
    
    1. Add Loader2 import at top of file:
    ```typescript
    import { CalendarRange, Loader2, Search, SlidersHorizontal } from "lucide-react";
    ```
    
    2. Add loading prop to interface:
    ```typescript
    interface TransactionsCommandBarProps {
      filters: TransactionsFilterState;
      activeFilterCount: number;
      loading: boolean;  // NEW
      onChange: (updates: Partial<TransactionsFilterState>) => void;
      onApply: () => void;
      onOpenAdvancedFilters: () => void;
    }
    ```
    
    3. Update function signature to receive loading:
    ```typescript
    export function TransactionsCommandBar({
      filters,
      activeFilterCount,
      loading,  // NEW
      onChange,
      onApply,
      onOpenAdvancedFilters
    }: TransactionsCommandBarProps) {
    ```
    
    4. Update Apply button (lines 150-158) to show spinner:
    ```typescript
    <button
      type="button"
      onClick={onApply}
      disabled={loading}  // NEW - disable during loading
      data-testid="txn-apply"
      aria-label="Apply search and filters"
      className="inline-flex h-11 items-center justify-center gap-2 rounded-2xl border border-emerald-500/30 bg-emerald-500/15 px-4 text-sm font-medium text-emerald-100 transition hover:bg-emerald-500/20 disabled:cursor-not-allowed disabled:opacity-60"
    >
      {loading ? <Loader2 className="h-4 w-4 animate-spin" /> : null}
      Apply
    </button>
    ```
    
    Note: The parent page.tsx must pass `loading` prop to TransactionsCommandBar. This will be done as part of integration.
  </action>
  <acceptance_criteria>
    - TransactionsCommandBar.tsx imports Loader2 from lucide-react
    - TransactionsCommandBarProps interface has `loading: boolean` prop
    - Apply button contains `<Loader2 className="h-4 w-4 animate-spin" />`
    - Apply button has `disabled={loading}` attribute
    - Apply button has `disabled:cursor-not-allowed disabled:opacity-60` classes
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Loader2" apps/web/src/app/transactions/TransactionsCommandBar.tsx | head -1 | grep -q "import"</automated>
  </verify>
  <done>Apply button shows spinner during async operations</done>
</task>

<task type="auto">
  <name>Task 3: Pass loading prop from page to TransactionsCommandBar</name>
  <files>apps/web/src/app/transactions/page.tsx</files>
  <read_first>
    - apps/web/src/app/transactions/page.tsx lines 910-920 (TransactionsCommandBar usage)
  </read_first>
  <action>
    Integrate loading prop into TransactionsCommandBar usage.
    
    At line 913-918, update the TransactionsCommandBar component call:
    ```typescript
    <TransactionsCommandBar
      filters={filters}
      activeFilterCount={activeFilterCount}
      loading={loading}  // NEW - pass loading state
      onChange={updateFilterDraft}
      onApply={applyFilters}
      onOpenAdvancedFilters={() => setShowAdvancedFilters(true)}
    />
    ```
    
    This connects the page's loading state to the command bar's Apply button spinner.
  </action>
  <acceptance_criteria>
    - page.tsx passes `loading={loading}` to TransactionsCommandBar
    - TransactionsCommandBar receives loading prop
  </acceptance_criteria>
  <verify>
    <automated>grep -n "loading=" apps/web/src/app/transactions/page.tsx | grep -q "TransactionsCommandBar"</automated>
  </verify>
  <done>Loading state flows from page to command bar Apply button</done>
</task>

</tasks>

<verification>
- Skeleton rows appear during table loading
- Apply button shows spinner when loading=true
- Apply button disabled during loading
</verification>

<success_criteria>
- User sees skeleton placeholders while data loads
- User sees spinner on Apply button during operations
- User cannot click Apply while loading
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-consistency-table-polish/02-03-SUMMARY.md`
</output>