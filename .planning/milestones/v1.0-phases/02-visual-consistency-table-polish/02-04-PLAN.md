---
phase: 02-visual-consistency-table-polish
plan: 04
type: execute
wave: 2
depends_on: [02-02, 02-03]
files_modified: [apps/web/src/app/transactions/page.tsx, apps/web/src/app/transactions/TransactionsAdvancedFilters.tsx, apps/web/src/app/transactions/TransactionsCommandBar.tsx]
autonomous: true
requirements: [VISL-04, UFBK-04]
user_setup: []
must_haves:
  truths:
    - "User sees consistent spacing patterns across all pages"
    - "User sees disabled buttons with visual indication (grayed out, cursor-not-allowed)"
    - "Disabled buttons have aria-disabled for accessibility"
  artifacts:
    - path: "apps/web/src/app/transactions/page.tsx"
      provides: "Consistent spacing and disabled button patterns"
      contains: "cursor-not-allowed, aria-disabled where applicable"
    - path: "apps/web/src/app/transactions/TransactionsAdvancedFilters.tsx"
      provides: "Apply button with proper disabled state"
      contains: "disabled:cursor-not-allowed"
  key_links:
    - from: "Multiple button elements"
      to: "Disabled state styling"
      via: "disabled:opacity-60 disabled:cursor-not-allowed"
      pattern: "disabled:cursor-not-allowed"
---

<objective>
Audit and ensure consistent spacing patterns and disabled state visual indication across Transactions page components.

Purpose: Users see polished, consistent UI with proper accessibility for disabled actions.
Output: Consistent spacing and disabled button styling with accessibility attributes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-consistency-table-polish/02-CONTEXT.md
@.planning/phases/02-visual-consistency-table-polish/02-RESEARCH.md

<interfaces>
<!-- Key patterns executor needs -->

From apps/web/src/app/accounts/page.tsx (reference for disabled styling):
```typescript
className="disabled:cursor-not-allowed disabled:opacity-60"
```

From apps/web/src/app/transactions/TransactionsAdvancedFilters.tsx lines 226-234:
```typescript
<button
  type="button"
  onClick={() => {
    setOpenMultiSelect(null);
    onApply();
  }}
  data-testid="txn-advanced-apply"
  className="inline-flex h-11 items-center justify-center rounded-2xl border border-emerald-500/30 bg-emerald-500/15 px-4 text-sm font-medium text-emerald-100 transition hover:bg-emerald-500/20"
>
  Apply filters
</button>
```

Current gaps from grep search:
- Transactions page already has cursor-not-allowed on some buttons (lines 880, 890, 1289, 1404, 1458)
- TransactionsAdvancedFilters Apply button missing disabled state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix spacing consistency (VISL-04)</name>
  <files>apps/web/src/app/transactions/page.tsx</files>
  <read_first>
    - apps/web/src/app/transactions/page.tsx (full file - scan for spacing patterns)
    - apps/web/src/app/explorer/page.tsx (compare spacing patterns)
    - apps/web/src/app/categories/page.tsx (compare spacing patterns)
  </read_first>
  <action>
    Per D-25, D-26, D-27 decisions: Audit and standardize spacing patterns.
    
    Focus areas:
    - Filter sections: px-5 py-5 for table cells (already consistent)
    - Table cells: px-5 py-5 pattern (already used)
    - Page headers: Check header section spacing
    
    Scan the transactions page for spacing patterns and ensure they match:
    - Table body cells: `px-5 py-5` (line ~1153-1268)
    - Table header: `px-5 py-3.5` (line ~1126-1143)
    - Command bar: `p-4` padding (line ~93)
    - Advanced filters: `p-5` padding (TransactionsAdvancedFilters.tsx line ~50)
    
    Expected pattern per Tailwind spacing scale:
    - Outer containers: p-4 to p-5
    - Table cells: px-5 py-5
    - Header cells: px-5 py-3.5 (slightly tighter)
    - Button padding: px-4 py-2 to px-4 py-3
    
    If inconsistencies found, document in SUMMARY. Most spacing appears consistent from Phase 01 work.
  </action>
  <acceptance_criteria>
    - Spacing patterns follow Tailwind spacing scale
    - Table cells use consistent px-5 py-5
    - Headers use px-5 py-3.5
    - Container padding uses p-4 or p-5
  </acceptance_criteria>
  <verify>
    <automated>grep -c "px-5 py-5" apps/web/src/app/transactions/page.tsx | grep -q "15"</automated>
  </verify>
  <done>Spacing patterns consistent across Transactions page</done>
</task>

<task type="auto">
  <name>Task 2: Add disabled state visual indication to buttons (UFBK-04)</name>
  <files>apps/web/src/app/transactions/TransactionsAdvancedFilters.tsx</files>
  <read_first>
    - apps/web/src/app/transactions/TransactionsAdvancedFilters.tsx lines 220-235 (Apply button)
    - apps/web/src/app/transactions/TransactionsCommandBar.tsx (Apply button updated in Plan 03)
  </read_first>
  <action>
    Per D-28, D-29, D-30 decisions: Ensure disabled buttons have visual indication and accessibility.
    
    Changes to TransactionsAdvancedFilters.tsx:
    
    1. Add disabled prop consideration to Apply button (line 226-234):
    
    Currently:
    ```typescript
    <button
      type="button"
      onClick={() => {
        setOpenMultiSelect(null);
        onApply();
      }}
      data-testid="txn-advanced-apply"
      className="inline-flex h-11 items-center justify-center rounded-2xl border border-emerald-500/30 bg-emerald-500/15 px-4 text-sm font-medium text-emerald-100 transition hover:bg-emerald-500/20"
    >
      Apply filters
    </button>
    ```
    
    Add disabled state styling (the parent component controls when to disable - typically during async operations):
    ```typescript
    <button
      type="button"
      onClick={() => {
        setOpenMultiSelect(null);
        onApply();
      }}
      disabled={false}  // Parent can pass disabled state if needed
      aria-disabled={false}  // Accessibility per D-30
      data-testid="txn-advanced-apply"
      className="inline-flex h-11 items-center justify-center rounded-2xl border border-emerald-500/30 bg-emerald-500/15 px-4 text-sm font-medium text-emerald-100 transition hover:bg-emerald-500/20 disabled:cursor-not-allowed disabled:opacity-60"
    >
      Apply filters
    </button>
    ```
    
    Note: The existing pattern `disabled:opacity-60` is already used in page.tsx (D-28). Adding `disabled:cursor-not-allowed` completes UFBK-04.
    
    2. Check Reset and Close buttons (lines 209-220) - they already have appropriate styling:
    ```typescript
    className="... disabled:opacity-60"  // Already has opacity reduction
    ```
    
    Add cursor-not-allowed to these buttons too:
    ```typescript
    className="... disabled:cursor-not-allowed disabled:opacity-60"
    ```
  </action>
  <acceptance_criteria>
    - TransactionsAdvancedFilters Apply button has `disabled:cursor-not-allowed`
    - TransactionsAdvancedFilters Apply button has `disabled:opacity-60`
    - Reset and Close buttons have `disabled:cursor-not-allowed`
  </acceptance_criteria>
  <verify>
    <automated>grep -c "disabled:cursor-not-allowed" apps/web/src/app/transactions/TransactionsAdvancedFilters.tsx | grep -q "3"</automated>
  </verify>
  <done>All buttons in Advanced Filters have proper disabled state styling</done>
</task>

<task type="auto">
  <name>Task 3: Ensure cursor-not-allowed on all disabled buttons in TransactionsCommandBar</name>
  <files>apps/web/src/app/transactions/TransactionsCommandBar.tsx</files>
  <read_first>
    - apps/web/src/app/transactions/TransactionsCommandBar.tsx (full file)
  </read_first>
  <action>
    Review all buttons in TransactionsCommandBar for disabled state styling.
    
    Buttons to check:
    - Apply button: Updated in Plan 03, should have `disabled:cursor-not-allowed disabled:opacity-60`
    - Clear all button (line ~139-147): Add `disabled:cursor-not-allowed disabled:opacity-60` if disabled state exists
    - Open Advanced Filters button (line ~160-173): No disabled state currently, add if needed
    
    For Clear all button (line 139-147), it's only shown when `activeFilterCount > 0`, so no explicit disabled state. No change needed.
    
    For Open Advanced Filters button (line 160-173), no disabled state. No change needed.
    
    Verify Apply button has both disabled classes (from Plan 03 update).
  </action>
  <acceptance_criteria>
    - Apply button in TransactionsCommandBar has `disabled:cursor-not-allowed disabled:opacity-60`
    - Other buttons either have disabled styling or don't need it
  </acceptance_criteria>
  <verify>
    <automated>grep "disabled:cursor-not-allowed" apps/web/src/app/transactions/TransactionsCommandBar.tsx | wc -l | grep -q "1"</automated>
  </verify>
  <done>TransactionsCommandBar buttons have proper disabled styling</done>
</task>

</tasks>

<verification>
- Spacing patterns consistent across page
- Disabled buttons have cursor-not-allowed and opacity-60
- Accessibility attributes present where needed
</verification>

<success_criteria>
- User sees consistent visual hierarchy across pages
- User sees clear disabled state indication
- Disabled buttons accessible with aria-disabled
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-consistency-table-polish/02-04-SUMMARY.md`
</output>