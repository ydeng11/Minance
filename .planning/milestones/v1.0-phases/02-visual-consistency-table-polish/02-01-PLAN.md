---
phase: 02-visual-consistency-table-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [apps/web/src/lib/constants.ts, apps/web/src/lib/constants.test.ts]
autonomous: true
requirements: [FILT-05, FILT-06, FILT-07]
user_setup: []
must_haves:
  truths:
    - "User can select 'This Month' preset to filter transactions from current month start to today"
    - "User can select 'This Year' preset to filter transactions from January 1 to today"
    - "User can select 'Custom' to trigger date picker UI"
    - "Date presets appear in both Explorer and Transactions filter dropdowns"
  artifacts:
    - path: "apps/web/src/lib/constants.ts"
      provides: "RANGE_OPTIONS constant with new presets"
      contains: "this_month, this_year, custom"
    - path: "apps/web/src/lib/constants.test.ts"
      provides: "Unit tests for RANGE_OPTIONS structure"
      exports: ["test suite"]
  key_links:
    - from: "apps/web/src/lib/constants.ts"
      to: "apps/web/src/app/transactions/TransactionsCommandBar.tsx"
      via: "import RANGE_OPTIONS"
      pattern: "RANGE_OPTIONS"
    - from: "apps/web/src/lib/constants.ts"
      to: "apps/web/src/app/explorer/components/FilterSidebar.tsx"
      via: "import RANGE_OPTIONS"
      pattern: "RANGE_OPTIONS"
---

<objective>
Extend RANGE_OPTIONS constant with date range presets (This Month, This Year, Custom) to enable quick filtering without manual date selection.

Purpose: Users can quickly select common date ranges instead of manually entering start/end dates.
Output: Extended RANGE_OPTIONS constant and unit test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-consistency-table-polish/02-CONTEXT.md
@.planning/phases/02-visual-consistency-table-polish/02-RESEARCH.md

<interfaces>
<!-- Key types and contracts executor needs -->

From apps/web/src/lib/constants.ts (current):
```typescript
export const RANGE_OPTIONS = [
  { value: "all", label: "All time" },
  { value: "30d", label: "Last 30 days" },
  { value: "90d", label: "Last 90 days" },
  { value: "365d", label: "Last 12 months" },
  { value: "ytd", label: "Year to date" }
] as const;
```

From apps/web/src/app/transactions/filters.ts:
```typescript
const RANGE_VALUES = new Set([...RANGE_OPTIONS.map((option) => option.value), "custom"]);
```

From apps/web/src/app/explorer/filters.ts:
```typescript
const RANGE_VALUES = new Set([...RANGE_OPTIONS.map((option) => option.value), "custom"]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RANGE_OPTIONS with new presets</name>
  <files>apps/web/src/lib/constants.ts</files>
  <read_first>
    - apps/web/src/lib/constants.ts (current RANGE_OPTIONS structure)
    - apps/web/src/app/transactions/filters.ts (RANGE_VALUES usage pattern)
    - apps/web/src/app/explorer/filters.ts (RANGE_VALUES usage pattern)
  </read_first>
  <action>
    Extend RANGE_OPTIONS array in apps/web/src/lib/constants.ts per D-06 decision:
    
    Current RANGE_OPTIONS has: all, 30d, 90d, 365d, ytd
    
    Add three new presets in this order (after existing entries, before any closing):
    1. `{ value: "this_month", label: "This Month" }` - current month from 1st to today
    2. `{ value: "this_year", label: "This Year" }` - January 1 to today
    3. `{ value: "custom", label: "Custom" }` - triggers date picker UI
    
    Note: Both TransactionsCommandBar.tsx and Explorer FilterSidebar.tsx already extend RANGE_OPTIONS with "custom" locally. Adding "custom" to the base constant will remove duplication.
    
    Final RANGE_OPTIONS order:
    - all, 30d, 90d, 365d, ytd, this_month, this_year, custom
    
    Per D-07: Presets apply to both Explorer and Transactions filter dropdowns automatically since they import from constants.ts.
  </action>
  <acceptance_criteria>
    - constants.ts contains `{ value: "this_month", label: "This Month" }`
    - constants.ts contains `{ value: "this_year", label: "This Year" }`
    - constants.ts contains `{ value: "custom", label: "Custom" }`
    - RANGE_OPTIONS array has 8 total entries (previously 5)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "value:" apps/web/src/lib/constants.ts | grep -q "8"</automated>
  </verify>
  <done>RANGE_OPTIONS extended with this_month, this_year, custom presets</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit tests for RANGE_OPTIONS</name>
  <files>apps/web/src/lib/constants.test.ts</files>
  <read_first>
    - apps/web/src/lib/constants.ts (RANGE_OPTIONS export)
    - apps/web/src/components/filters/ActiveFilterBadges.test.ts (test pattern reference)
  </read_first>
  <behavior>
    - Test 1: RANGE_OPTIONS exports as array with expected length
    - Test 2: Each preset has value and label properties
    - Test 3: this_month, this_year, custom values present
    - Test 4: All values are unique (no duplicates)
  </behavior>
  <action>
    Create apps/web/src/lib/constants.test.ts with Node.js built-in test runner pattern:
    
    ```typescript
    import { describe, it } from "node:test";
    import assert from "node:assert/strict";
    import { RANGE_OPTIONS } from "./constants.js";
    
    describe("RANGE_OPTIONS", () => {
      it("exports array with expected presets", () => {
        assert.ok(Array.isArray(RANGE_OPTIONS));
        assert.ok(RANGE_OPTIONS.length >= 8);
      });
      
      it("each preset has value and label", () => {
        for (const option of RANGE_OPTIONS) {
          assert.ok(typeof option.value === "string");
          assert.ok(typeof option.label === "string");
        }
      });
      
      it("contains new presets this_month, this_year, custom", () => {
        const values = RANGE_OPTIONS.map(o => o.value);
        assert.ok(values.includes("this_month"));
        assert.ok(values.includes("this_year"));
        assert.ok(values.includes("custom"));
      });
      
      it("all values are unique", () => {
        const values = RANGE_OPTIONS.map(o => o.value);
        const unique = new Set(values);
        assert.strictEqual(values.length, unique.size);
      });
    });
    ```
    
    Note: Use .js extension in import per ESM module pattern in project.
  </action>
  <acceptance_criteria>
    - File apps/web/src/lib/constants.test.ts exists
    - File contains `import { RANGE_OPTIONS } from "./constants.js"`
    - File contains at least 4 test cases
    - `pnpm test` passes with new test file
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- --test-name-pattern="RANGE_OPTIONS"</automated>
  </verify>
  <done>Unit tests verify RANGE_OPTIONS structure and new presets</done>
</task>

</tasks>

<verification>
- RANGE_OPTIONS contains this_month, this_year, custom values
- Unit tests pass
- No duplication of "custom" in TransactionCommandBar.tsx (can remove local extension)
</verification>

<success_criteria>
- User can select "This Month" preset in filter dropdown
- User can select "This Year" preset in filter dropdown
- User can select "Custom" to trigger date picker
- Presets appear in both Explorer and Transactions
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-consistency-table-polish/02-01-SUMMARY.md`

Note: FILT-06 and FILT-07 are already implemented - MultiSelectField is used for categories and accounts in both TransactionsAdvancedFilters.tsx and ExplorerAdvancedFilters.tsx. No additional work required.
</output>