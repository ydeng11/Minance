---
phase: 03-interaction-patterns
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/components/providers/AppProviders.tsx
  - apps/web/src/app/transactions/page.tsx
  - e2e/specs/transactions-bulk-delete.spec.ts
autonomous: true
requirements: [INTX-03]
---

<objective>
Add Sonner toast infrastructure and migrate Transactions page feedback from inline `global-message` to toasts (success, error, warning, neutral message).
</objective>

<tasks>
<task type="auto">Add `sonner` dependency; render `<Toaster />` in `AppProviders` with dark theme.</task>
<task type="auto">Replace `message` state and `data-testid="global-message"` block on Transactions page with `toast.*` calls; simplify `loadTransactions` / `refreshTransactionsView` (remove preserveMessage).</task>
<task type="auto">Update `transactions-bulk-delete` E2E to assert visible toast text.</task>
</tasks>

<output>
After completion: `03-01-SUMMARY.md`
</output>
