---
phase: 04-user-feedback-error-handling
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/web/src/app/recurrings/page.tsx
  - apps/web/src/app/recurrings/page.test.ts
  - e2e/specs/cross-tab-parity.spec.ts
autonomous: true
requirements: [UFBK-01, UFBK-02]
must_haves:
  truths:
    - "Recurrings form validation errors appear next to the offending field instead of only in the page-level alert."
    - "Recurrings routine success actions use Sonner toasts and no longer occupy the persistent inline alert region."
    - "Recurrings request failures remain visible inline with recovery-oriented copy and preserved draft input."
  artifacts:
    - path: apps/web/src/app/recurrings/page.tsx
      provides: "Separated validation, page-error, and toast-success surfaces for Recurrings."
    - path: apps/web/src/app/recurrings/page.test.ts
      provides: "Automated coverage for field validation and page-error separation."
    - path: e2e/specs/cross-tab-parity.spec.ts
      provides: "User-path assertions for Recurrings toast success behavior."
  key_links:
    - from: apps/web/src/app/recurrings/page.tsx
      to: apps/web/src/lib/feedback/requestFeedback.ts
      via: "shared fallback helper from 04-01"
      pattern: "get.*Error|build.*Feedback"
    - from: apps/web/src/app/recurrings/page.tsx
      to: e2e/specs/cross-tab-parity.spec.ts
      via: "Recurring create and evaluate user-visible messages"
      pattern: "Recurring rule created\\.|Evaluation complete\\."
---

<objective>
Bring the Recurrings page in line with the Phase 4 contract by separating field validation from page-level failures and moving routine success messages to Sonner toasts.

Purpose: Finish UFBK-01 and UFBK-02 for the second scoped workflow page without expanding Phase 4 beyond Import and Recurrings.
Output: Updated Recurrings page state model, page-level tests, and migrated cross-tab parity assertions.
</objective>

<execution_context>
@/Users/ihelio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ihelio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-user-feedback-error-handling/04-CONTEXT.md
@.planning/phases/04-user-feedback-error-handling/04-RESEARCH.md
@.planning/phases/04-user-feedback-error-handling/04-UI-SPEC.md
@.planning/phases/03-interaction-patterns/03-01-SUMMARY.md
@.planning/phases/03-interaction-patterns/03-02-SUMMARY.md
@.planning/phases/04-user-feedback-error-handling/04-01-PLAN.md
@apps/web/src/app/recurrings/page.tsx
@e2e/specs/cross-tab-parity.spec.ts

<interfaces>
From `apps/web/src/app/recurrings/page.tsx`:
```typescript
const [createDraft, setCreateDraft] = useState({
  name: "",
  cadence: "monthly",
  amount: "",
  direction: ""
});

const [editDraft, setEditDraft] = useState({
  name: "",
  cadence: "monthly",
  amount: "",
  merchant_pattern: "",
  category_final: "",
  account_id: "",
  direction: ""
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split Recurrings validation from page errors and add page-level tests</name>
  <files>apps/web/src/app/recurrings/page.tsx, apps/web/src/app/recurrings/page.test.ts</files>
  <read_first>
    .planning/phases/04-user-feedback-error-handling/04-CONTEXT.md
    .planning/phases/04-user-feedback-error-handling/04-RESEARCH.md
    .planning/phases/04-user-feedback-error-handling/04-UI-SPEC.md
    apps/web/src/app/recurrings/page.tsx
    apps/web/src/lib/feedback/requestFeedback.ts
    apps/web/src/app/transactions/form.ts
  </read_first>
  <behavior>
    - Test 1: submitting create with a blank name shows adjacent field text for the name field and preserves the rest of the draft.
    - Test 2: submitting create or edit with a missing, non-numeric, or `<= 0` amount shows adjacent amount-field feedback and does not replace it with a global success message.
    - Test 3: request-level failures still populate the page-level inline alert with recovery-oriented copy.
  </behavior>
  <action>Create `apps/web/src/app/recurrings/page.test.ts` and cover the field-validation split called out in research Wave 0. Update `apps/web/src/app/recurrings/page.tsx` so `message` is replaced with separate state for field errors and page-level request errors. Per D-02 and the UI spec, add field-level guidance for the exact obvious problems `Rule name is required.` and `Recurring amount must be greater than zero.` adjacent to the create/edit controls using destructive text styling and preserved draft input. Keep `data-testid="global-message"` for page-level request failures or persistent context only; do not use it for routine validation failures.</action>
  <acceptance_criteria>
    - `apps/web/src/app/recurrings/page.test.ts` exists.
    - `apps/web/src/app/recurrings/page.tsx` defines distinct state for field validation and page-level errors instead of a single `message` state.
    - The rendered form includes adjacent validation text for missing name and invalid amount.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test apps/web/src/app/recurrings/page.test.ts</automated>
  </verify>
  <done>Recurrings validation errors are field-owned, drafts are preserved, and page-level alerts are reserved for request or cross-field failures.</done>
</task>

<task type="auto">
  <name>Task 2: Move Recurrings success states to toasts and migrate the parity spec</name>
  <files>apps/web/src/app/recurrings/page.tsx, e2e/specs/cross-tab-parity.spec.ts</files>
  <read_first>
    .planning/phases/04-user-feedback-error-handling/04-CONTEXT.md
    .planning/phases/04-user-feedback-error-handling/04-UI-SPEC.md
    .planning/phases/03-interaction-patterns/03-01-SUMMARY.md
    apps/web/src/app/recurrings/page.tsx
    e2e/specs/cross-tab-parity.spec.ts
  </read_first>
  <action>Update `apps/web/src/app/recurrings/page.tsx` to import `toast` from `sonner` and use the exact UI-spec success copy for create, save edits, evaluate, pause, resume, archive, and delete. Use `Recurring rule created.`, `Recurring rule updated.`, `Evaluation complete. {N} matches found.`, `Recurring rule paused.`, `Recurring rule resumed.`, `Recurring rule archived.`, and `Recurring rule deleted.`. Keep inline page alerts for load/detail/request failures only, with calm recovery-oriented fallback copy produced by the helper from 04-01. Preserve current loading/disabled patterns. Update `e2e/specs/cross-tab-parity.spec.ts` so the Recurrings path asserts visible toast text for create and evaluate instead of expecting `global-message` success content.</action>
  <acceptance_criteria>
    - `apps/web/src/app/recurrings/page.tsx` imports `toast` from `sonner`.
    - The file contains the exact strings `Recurring rule created.` and `Recurring rule deleted.`.
    - `e2e/specs/cross-tab-parity.spec.ts` no longer asserts `global-message` for Recurrings success paths.
    - `data-testid="global-message"` remains available in the Recurrings page for blocking page-level issues.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test apps/web/src/app/recurrings/page.test.ts</automated>
  </verify>
  <done>Recurrings routine successes are transient toasts, failures remain inline and actionable, and the parity spec matches the new contract.</done>
</task>

</tasks>

<verification>
Run the Recurrings page test after each task commit. At wave completion, run the cross-tab E2E spec. Before summary or handoff, run `just check`. Confirm the page-level alert remains available for load/detail/request failures, but create/evaluate success is now observable through toast text.
</verification>

<success_criteria>
Recurrings users can tell immediately whether a field needs correction or an action completed successfully, and they never have to decode a generic page-wide message for simple form mistakes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-feedback-error-handling/04-02-SUMMARY.md`
</output>
