---
phase: 04-user-feedback-error-handling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/feedback/requestFeedback.ts
  - apps/web/src/lib/import/reducer.ts
  - apps/web/src/app/import/page.tsx
  - apps/web/src/app/import/page.test.ts
  - e2e/specs/import-existing-account-transactions.spec.ts
autonomous: true
requirements: [UFBK-01, UFBK-02]
must_haves:
  truths:
    - "Import success actions use Sonner toasts instead of the persistent inline alert."
    - "Import failures stay visible in the page-level `data-testid=\"global-message\"` region with recovery-oriented copy."
    - "Import account assignment and reconciliation mapping still leave users with next-step guidance after success."
  artifacts:
    - path: apps/web/src/lib/feedback/requestFeedback.ts
      provides: "Small helper for action-specific fallback copy built on ApiError messaging."
    - path: apps/web/src/app/import/page.tsx
      provides: "Import feedback routing for analyze, save mapping, account assignment, reconciliation mapping, and commit."
    - path: apps/web/src/app/import/page.test.ts
      provides: "Automated coverage for helper/reducer or import feedback-surface rules."
    - path: e2e/specs/import-existing-account-transactions.spec.ts
      provides: "User-path assertions for Import toast copy and inline guidance."
  key_links:
    - from: apps/web/src/app/import/page.tsx
      to: apps/web/src/lib/feedback/requestFeedback.ts
      via: "shared error fallback helper"
      pattern: "get.*Error|build.*Feedback"
    - from: apps/web/src/app/import/page.tsx
      to: apps/web/src/lib/import/reducer.ts
      via: "dispatch analyze/mapping/commit actions"
      pattern: "dispatch\\(\\{ type: \"(analyze|mapping_save|commit)_"
    - from: e2e/specs/import-existing-account-transactions.spec.ts
      to: apps/web/src/app/import/page.tsx
      via: "toast text and `global-message` assertions"
      pattern: "Import analyzed\\.|Mapping saved\\.|Import committed\\.|global-message"
---

<objective>
Move Import feedback to the Phase 4 contract: success confirmations become Sonner toasts, blocking errors stay inline with recovery guidance, and the existing workflow reducer keeps ownership of step state.

Purpose: Deliver UFBK-01 and UFBK-02 for the Import workflow without broad app-wide refactors.
Output: Shared feedback helper, updated Import page behavior, and matching automated tests.
</objective>

<execution_context>
@/Users/ihelio/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ihelio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-user-feedback-error-handling/04-CONTEXT.md
@.planning/phases/04-user-feedback-error-handling/04-RESEARCH.md
@.planning/phases/04-user-feedback-error-handling/04-UI-SPEC.md
@.planning/phases/03-interaction-patterns/03-01-SUMMARY.md
@apps/web/src/app/import/page.tsx
@apps/web/src/lib/import/reducer.ts
@apps/web/src/lib/api/client.ts
@apps/web/src/app/import/page.test.ts
@e2e/specs/import-existing-account-transactions.spec.ts

<interfaces>
From `apps/web/src/lib/api/client.ts`:
```typescript
export class ApiError extends Error {
  status: number;
  payload: ApiErrorPayload | null;
}
```

From `apps/web/src/lib/import/reducer.ts`:
```typescript
export interface ImportWorkflowState {
  error: string | null;
  notice: string | null;
}

export type ImportWorkflowAction =
  | { type: "analyze_succeeded"; importJob: ImportJob; details: ImportDetailsResponse; processedRows: ImportProcessedRowsResponse }
  | { type: "mapping_save_succeeded"; details: ImportDetailsResponse; processedRows: ImportProcessedRowsResponse }
  | { type: "commit_succeeded"; result: CommitImportResponse }
  | { type: "error"; message: string };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the small shared feedback helper and Import-focused test coverage</name>
  <files>apps/web/src/lib/feedback/requestFeedback.ts, apps/web/src/lib/import/reducer.ts, apps/web/src/app/import/page.test.ts</files>
  <read_first>
    .planning/phases/04-user-feedback-error-handling/04-CONTEXT.md
    .planning/phases/04-user-feedback-error-handling/04-RESEARCH.md
    .planning/phases/04-user-feedback-error-handling/04-UI-SPEC.md
    apps/web/src/lib/api/client.ts
    apps/web/src/lib/import/reducer.ts
    apps/web/src/app/import/page.tsx
    apps/web/src/app/import/page.test.ts
  </read_first>
  <behavior>
    - Test 1: unknown Import failures use action-specific fallback copy that states what failed, says nothing changed when true, and ends with one recovery step.
    - Test 2: reducer success actions no longer require the inline alert region to carry routine success text if the page routes those states to toasts.
  </behavior>
  <action>Create `apps/web/src/lib/feedback/requestFeedback.ts` with a small exported helper that prefers `ApiError.message` and `payload.error.details.remediation`, but accepts concrete fallback copy strings from the UI spec such as `Import analysis couldn’t finish. Nothing changed. Check the file format and try again.` and `Import commit couldn’t be completed. Nothing changed. Review the remaining issues, then commit again.` Per D-01 and D-02, do not build a notification framework; keep the helper limited to formatting fallback/recovery wording. Adjust `apps/web/src/lib/import/reducer.ts` only as needed so analyze/save/commit state transitions can stop treating `notice` as the primary success surface while preserving reducer ownership of workflow status. Expand `apps/web/src/app/import/page.test.ts` from the current toolbar-only assertion into focused coverage for the helper/reducer behavior this plan introduces.</action>
  <acceptance_criteria>
    - `apps/web/src/lib/feedback/requestFeedback.ts` exists and exports a helper used for Import fallback wording.
    - `apps/web/src/app/import/page.test.ts` contains assertions for recovery-oriented Import copy or reducer feedback routing, not only the toolbar test.
    - No new dependency is added to `apps/web/package.json` or workspace manifests.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test apps/web/src/app/import/page.test.ts</automated>
  </verify>
  <done>Import has a reusable, tested fallback-copy helper that is small enough for this phase and ready to be consumed by the page handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Route Import success and error feedback to the correct surfaces and migrate the E2E assertions</name>
  <files>apps/web/src/app/import/page.tsx, e2e/specs/import-existing-account-transactions.spec.ts</files>
  <read_first>
    .planning/phases/04-user-feedback-error-handling/04-CONTEXT.md
    .planning/phases/04-user-feedback-error-handling/04-UI-SPEC.md
    .planning/phases/03-interaction-patterns/03-01-SUMMARY.md
    apps/web/src/app/import/page.tsx
    apps/web/src/lib/feedback/requestFeedback.ts
    apps/web/src/lib/import/reducer.ts
    e2e/specs/import-existing-account-transactions.spec.ts
  </read_first>
  <action>Update `apps/web/src/app/import/page.tsx` so the critical success paths in D-01 and the UI spec call `toast.success` with the exact success copy: `Import analyzed. Review the preview before saving mapping.`, `Mapping saved. Continue to account review.`, `Import account assigned. Review any remaining unmatched rows.`, `Reconciliation account mapped. Check the remaining issues before commit.`, and `Import committed. Review imported transactions if anything looks off.` Keep the stable inline alert region at `data-testid="global-message"` for blocking errors and persistent workflow guidance only, with action-specific fallback strings for analyze, mapping save, account assignment, reconciliation mapping, row update, and commit failures. Preserve inline next-step guidance where account assignment or reconciliation still needs user attention, per D-03 and the UI-spec allowance for mixed toast + inline guidance. Update `e2e/specs/import-existing-account-transactions.spec.ts` to assert the new toast text for analyze, mapping save, import-account assignment, and commit, while retaining inline-region assertions only for truly persistent guidance or blocking failures.</action>
  <acceptance_criteria>
    - `apps/web/src/app/import/page.tsx` imports `toast` from `sonner`.
    - The file contains the exact success strings `Import analyzed. Review the preview before saving mapping.` and `Import committed. Review imported transactions if anything looks off.`.
    - `data-testid="global-message"` remains in `apps/web/src/app/import/page.tsx`.
    - `e2e/specs/import-existing-account-transactions.spec.ts` asserts user-facing toast text instead of `global-message` for routine Import success paths.
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsx --test apps/web/src/app/import/page.test.ts</automated>
  </verify>
  <done>Import success confirmations are transient toasts, blocking problems stay inline with recovery guidance, and the Import E2E flow matches the new feedback contract.</done>
</task>

</tasks>

<verification>
Run the focused Import unit test after each task commit. At wave completion, run the Import E2E spec. Before summary or handoff, run `just check`. Confirm routine success no longer depends on `global-message`, while blocking guidance and errors still render there.
</verification>

<success_criteria>
Import users receive lightweight success confirmations for each critical step, but still get persistent, recovery-oriented inline guidance whenever the workflow cannot proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-feedback-error-handling/04-01-SUMMARY.md`
</output>
